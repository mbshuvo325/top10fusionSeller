import 'package:flutter/material.dart';

class KeyPage extends StatelessWidget {
  const KeyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Please contact with MB Shuvo for key"),
      ),
    );
  }
}
